# Telegram-бот с Narrative Engine

Telegram-бот для интерактивных текстовых историй с ветвлениями, выбором игрока и финалами.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка

Создайте файл `.env` или установите переменные окружения:

```bash
export BOT_TOKEN="your_telegram_bot_token"
export ADMIN_USER_IDS="123456789,987654321"  # ID администраторов через запятую
export DEBUG="False"  # или "True" для отладки
export DB_PATH="bot.db"  # путь к БД (опционально)
export STORIES_DIR="stories"  # путь к директории с историями (опционально)
```

### 3. Запуск

```bash
python bot.py
```

## 📁 Структура проекта

```
project_root/
├── bot.py                 # Основной файл бота
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости
│
├── engine/                # Движок истории
│   ├── story_engine.py    # Основная логика
│   ├── scene_renderer.py  # Рендеринг сцен
│   ├── conditions.py      # Обработка условий
│   └── effects.py         # Обработка эффектов
│
├── storage/               # Работа с БД
│   ├── db.py             # Инициализация БД
│   ├── models.py         # Модели данных
│   └── repository.py     # Репозиторий
│
├── stories/               # YAML-файлы историй
│   └── example_story.yaml
│
├── admin/                 # Админ-команды
│   └── commands.py
│
└── utils/                 # Утилиты
    └── logger.py
```

## 📖 Команды бота

### Для пользователей:
- `/start` - Приветствие и список историй
- `/list` - Список доступных историй
- `/play <story_id>` - Начать историю

### Для администраторов:
- `/start_story <story_id> [user_id]` - Запустить историю для пользователя
- `/reset_story <user_id> <story_id>` - Сбросить попытку прохождения
- `/preview_scene <story_id> <scene_id>` - Предпросмотр сцены
- `/active_runs` - Список активных попыток прохождения

## 📝 Формат истории (YAML)

Каждая история описывается в отдельном YAML-файле:

```yaml
id: story_id
title: "Название истории"
description: "Описание"
version: 1.0
start_scene: intro
allow_restart: true

scenes:
  intro:
    text: |
      Текст сцены
    choices:
      - id: choice_id
        text: "Текст выбора"
        next_scene: next_scene_id
        conditions:  # опционально
          - has_flag: flag_name
        effects:  # опционально
          - add_flag: flag_name

endings:
  ending_id:
    text: |
      Текст финала
    ending_type: success  # success, failure, neutral
```

## 🧩 Условия

- `has_flag: flag_name` - проверка наличия флага
- `not_has_flag: flag_name` - проверка отсутствия флага
- `flag_equals: {flag: flag_name, value: value}` - проверка значения флага

## ✨ Эффекты

- `add_flag: flag_name` - добавить флаг со значением "1"
- `remove_flag: flag_name` - удалить флаг
- `set_flag: {flag: flag_name, value: value}` - установить флаг с конкретным значением
- `increment_counter: counter_name` - увеличить счётчик на 1

## 🗄️ База данных

Используется SQLite с тремя таблицами:
- `users` - пользователи
- `runs` - попытки прохождения
- `flags` - флаги для каждой попытки

## 🐞 Режим отладки

При `DEBUG=True` в сообщениях отображается:
- ID текущей сцены
- Run ID
- Текущие флаги

## 📝 Лицензия

Проект создан в соответствии с техническим заданием.
