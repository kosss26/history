# Руководство администратора

Руководство по использованию админ-панели для управления историями в Telegram-боте.

## 🔐 Доступ

Все админ-команды доступны только пользователям, чьи ID указаны в `ADMIN_USER_IDS` в `config.py`.

## 📋 Главное меню

Используйте команду `/admin` для просмотра списка доступных команд.

## 📚 Управление историями

### Список историй

```
/admin_stories
```

Показывает список всех загруженных историй с информацией о количестве сцен и финалов.

### Редактирование истории

#### Редактирование текста сцены

```
/admin_edit_text <story_id> <scene_id>
```

Режимы ввода:
1. **Одним сообщением**: Отправьте новый текст сразу после команды
2. **Многострочный режим**: 
   - Отправьте `добавить строку`
   - Отправляйте строки текста по одной
   - Отправьте `завершить` для сохранения

**Пример:**
```
/admin_edit_text detective_case_01 intro
[Отправляете новый текст]
```

Или:
```
/admin_edit_text detective_case_01 intro
добавить строку
Первая строка текста
Вторая строка текста
завершить
```

### Управление выбором

#### Список выборов

```
/admin_choices <story_id> <scene_id>
```

Показывает все выборы в указанной сцене.

#### Добавление выбора

```
/admin_add_choice <story_id> <scene_id>
```

Процесс добавления:
1. Введите ID выбора (уникальный идентификатор)
2. Введите текст выбора
3. Выберите следующую сцену/финал:
   - Используйте inline-кнопки для выбора из списка
   - Или введите ID вручную

**Пример:**
```
/admin_add_choice detective_case_01 intro
choice_1
[ID выбора]
Исследовать сообщение
[Текст выбора]
[Выбираете сцену из кнопок или вводите вручную]
```

#### Удаление выбора

```
/admin_delete_choice <story_id> <scene_id> <choice_id>
```

**Пример:**
```
/admin_delete_choice detective_case_01 intro choice_1
```

## 📤 Загрузка истории

### Загрузка YAML

```
/admin_upload
```

Два способа загрузки:

#### 1. Текстом

После команды `/admin_upload` отправьте YAML текстом в сообщении:

```yaml
id: my_story
title: "Моя история"
start_scene: intro
scenes:
  intro:
    text: |
      Текст сцены
    choices:
      - id: choice1
        text: "Выбор 1"
        next_scene: scene2
endings:
  ending1:
    text: "Финал"
    ending_type: success
```

#### 2. Файлом

Отправьте файл `.yaml` или `.yml` после команды `/admin_upload`.

**Важно:**
- YAML должен содержать поле `id` с уникальным идентификатором истории
- Если история с таким ID уже существует, бот запросит подтверждение перезаписи
- После загрузки история автоматически валидируется
- При успешной загрузке показывается сводка: ID, название, версия, количество сцен и финалов

## 📥 Экспорт истории

```
/admin_export <story_id>
```

Экспортирует историю в формате YAML:
- Если текст помещается в сообщение (≤4096 символов) - отправляется как текст
- Если текст длиннее - отправляется как файл `.yaml`

**Пример:**
```
/admin_export detective_case_01
```

## 🗑️ Удаление истории

```
/admin_delete <story_id>
```

**Важно:**
- Требуется подтверждение (кнопки "Да/Нет")
- История не удаляется полностью, а перемещается в папку `stories/_deleted/`
- Это безопаснее и позволяет восстановить историю при необходимости

**Пример:**
```
/admin_delete old_story
[Подтверждаете удаление кнопкой]
```

## 🔄 Перезагрузка историй

```
/admin_reload
```

Перезагружает все истории из файлов в директории `stories/`. Используйте после:
- Ручного редактирования файлов
- Добавления новых файлов
- Изменения файлов вне бота

## ✅ Валидация истории

```
/admin_validate <story_id>
```

Проверяет историю на ошибки и предупреждения:

**Проверяется:**
- Наличие обязательных полей (`id`, `start_scene`)
- Существование всех ссылок на сцены и финалы
- Наличие текста в сценах и финалах
- Корректность структуры выборов

**Результат:**
- ✅ История валидна - ошибок нет
- ⚠️ История валидна, но есть предупреждения
- ❌ История содержит ошибки (список ошибок)

**Пример:**
```
/admin_validate detective_case_01
```

## 🔍 Preview истории

```
/admin_preview <story_id>
```

Запускает историю в режиме предпросмотра:
- История запускается как для обычного игрока
- Можно протестировать все ветки и выборы
- В сообщениях отображается пометка `[PREVIEW MODE]` и Run ID
- Данные сохраняются в БД, но помечаются как preview

**Пример:**
```
/admin_preview detective_case_01
[Проходите историю как игрок]
```

## 📝 Старые команды

Следующие команды остаются доступными:

- `/start_story <story_id> [user_id]` - Запустить историю для пользователя
- `/reset_story <user_id> <story_id>` - Сбросить попытку прохождения
- `/preview_scene <story_id> <scene_id>` - Предпросмотр одной сцены
- `/active_runs` - Список активных попыток прохождения

## 🛡️ Безопасность

### Защита от path traversal

- Все `story_id` валидируются и очищаются от опасных символов
- Запрещены символы: `/`, `\`, `..`
- Разрешены только: буквы, цифры, `_`, `-`

### Ограничения

- Максимальная длина текста в одном сообщении: 8000 символов
- Для длинных текстов используйте многострочный режим
- YAML файлы валидируются перед сохранением
- Некорректный YAML не сохраняется, возвращается описание ошибки

## 💡 Советы

1. **Всегда валидируйте** историю после редактирования: `/admin_validate <story_id>`
2. **Используйте preview** для тестирования перед публикацией
3. **Экспортируйте** истории перед большими изменениями как резервную копию
4. **Перезагружайте** истории после ручного редактирования файлов: `/admin_reload`
5. **Проверяйте ссылки** - все `next_scene` должны вести на существующие сцены или финалы

## 📋 Формат истории (YAML)

```yaml
id: story_id                    # Обязательно: уникальный ID
title: "Название"               # Опционально
description: "Описание"          # Опционально
version: 1.0                     # Опционально
start_scene: intro              # Обязательно: ID стартовой сцены
allow_restart: true             # Опционально: разрешить перезапуск

scenes:
  intro:
    text: |                     # Текст сцены
      Многострочный текст
    choices:                    # Список выборов
      - id: choice1
        text: "Текст выбора"
        next_scene: scene2      # ID следующей сцены или финала
        conditions:             # Опционально: условия
          - has_flag: flag_name
        effects:                 # Опционально: эффекты
          - add_flag: flag_name

endings:
  ending1:
    text: |                     # Текст финала
      Текст финала
    ending_type: success        # success, failure, neutral
```

## 🐛 Решение проблем

### История не загружается

1. Проверьте формат YAML (используйте валидатор)
2. Убедитесь, что поле `id` присутствует
3. Проверьте, что все ссылки на сцены существуют

### Ошибка сохранения

1. Проверьте права доступа к папке `stories/`
2. Убедитесь, что `story_id` содержит только разрешённые символы
3. Проверьте размер файла (не должен быть слишком большим)

### Выбор не работает

1. Проверьте, что `next_scene` указывает на существующую сцену/финал
2. Убедитесь, что условия (если есть) корректны
3. Используйте валидацию для проверки структуры
